import { ThemeConfig } from '@/types/themeConfig'

const themeConfig: ThemeConfig = {
  app: { name: 'List' },
  mode: 'light',
  routingLoader: true,
  themeColor: '#288afd',
  icon: '/logo/logo.png',
}

export default themeConfig
